schemaVersion: att-template/v2.1
name: PAYMENT_PREPARE
description: Prepare payment request identifiers.

actions:
  getAcDate:
    type: tool
    call: "#{getAcDate()}"

  getSeq:
    type: tool
    call: "#{getSeq(caseId=${CASE.caseId})}"

  genEndToEndId:
    type: tool
    call: "#{genEndToEndId(caseId=${CASE.caseId})}"

  logIds:
    type: log
    level: INFO
    message: "Prepared IDs for ${CASE.caseId}"
    fields:
      acDate: "${ACTIONS.getAcDate.output}"
      sequence: "${ACTIONS.getSeq.output}"
      endToEndId: "${ACTIONS.genEndToEndId.output}"
