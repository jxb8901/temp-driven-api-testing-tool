schemaVersion: att-flow/v3.0
id: common.identity.v1
name: Identity
description: V3.2 Flow reading Case data through the same Context used by Template Actions.
actions:
  identityValue:
    type: tool
    call: "#{misc.string(value=${CASE.caseId})}"
