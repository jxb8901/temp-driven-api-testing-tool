schemaVersion: att-flow/v3.0
id: common.compose.v1
name: Compose Value
description: V3.2 statically nested Flow composition with one shared ACTIONS Context.
actions:
  identityFlow:
    type: flow
    use: common.identity.v1
  decorateFlow:
    type: flow
    use: common.decorate.v1
