name: PAYMENT_INVOKE
description: Render payment request XML and invoke payment API

actions:
  renderRequest:
    type: render
    payload: payload.xml

  invokeApi:
    type: tool
    call: "#{invokePaymentApi(requestXml=${TOOLS.renderRequest.output})}"
