name: PAYMENT_PREPARE
description: Prepare payment request IDs

actions:
  getAcDate:
    type: tool
    call: "#{getAcDate()}"

  getSeq:
    type: tool
    call: "#{getSeq(caseId=${CaseID})}"

  genEndToEndId:
    type: tool
    call: "#{genEndToEndId(caseId=${CaseID})}"
