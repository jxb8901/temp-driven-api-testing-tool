schemaVersion: att-load/v1.0

target:
  type: tool
  id: sample.getAcDate

load:
  users: 1
  duration: 2s

# The grouped sample Tool has no arguments. Replace it with an integration
# Tool and its arguments when using this scenario against a real service.
